apiVersion: 1

providers:
  - name: 'ModelServe Dashboards'
    orgId: 1
    folder: 'ModelServe'
    folderUid: 'modelserve'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards