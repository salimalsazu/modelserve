FROM python:3.10-slim

WORKDIR /app

# Install MLflow and Kaggle for training
RUN pip install --no-cache-dir \
    mlflow>=2.14.0 \
    kaggle>=1.5.0 \
    psycopg2-binary \
    gunicorn \
    boto3

# Default command is already set in the base image